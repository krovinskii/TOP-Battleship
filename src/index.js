import "./styles.css";
import { listener } from "./eventListners";

listener.init();
//type is undefined --bug-- they place different numbers --bug--
//Need to alert when player has won
//Need to record hits and misses for each player
